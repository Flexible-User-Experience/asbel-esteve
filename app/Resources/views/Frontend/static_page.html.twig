{% extends ':Frontend:layout.html.twig' %}

{% block title %}{{ page.title }} · {{ parent() }}{% endblock %}

{% block body_class %}container{% endblock %}

{% block body %}
    <div class="row">
        <div class="my-padding col-xs-12">
            <h1>{{ page.title }}</h1>
            <p>{{ page.description | raw }}</p>
        </div>
    </div>
{% endblock %}
